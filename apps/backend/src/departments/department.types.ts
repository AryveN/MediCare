export type Department = {
  id: string;
  name: string;
  doctorsList: string[];
  capacity: number;
  contact: string;
  note?: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
};
